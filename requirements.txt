# AnimeAIDub Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: torch + torchaudio should be installed separately with CUDA support:
#   pip install torch==2.4.0+cu124 torchaudio==2.4.0+cu124 \
#     --extra-index-url https://download.pytorch.org/whl/cu124

# Audio source separation
demucs>=4.0.1

# Audio I/O
soundfile>=0.12.0
torchaudio>=2.4.0

# Speech recognition (Whisper for JP transcription)
transformers>=4.51.0
openai-whisper>=20231117

# Model downloads
huggingface-hub>=0.20.0

# Core
numpy==1.26.4
torch>=2.4.0
pyyaml>=6.0

# CosyVoice3 dependencies (also installed via CosyVoice repo)
# These are listed here for reference — the Docker image installs
# them from the CosyVoice requirements directly.
# conformer
# diffusers
# HyperPyYAML
# omegaconf
# WeTextProcessing
